#' Subset data for heatmap prepared by prepare_data_for_heatmap.
#' 
#'
#' @param  clusterTermsBy string indicating a metric to use to cluster GO terms.
prepare_data_for_heatmap2 <- function(d,
                                      metrics,
                                      ontologies){
    melted <- 'todo'
    return(melted)
}
